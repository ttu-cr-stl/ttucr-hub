"use client";
import { useUser } from "@/lib/hooks/useUser";
import { FC } from "react";


export const UpdateProfile = () => {
  const { user } = useUser();

  return <div className="">jhfkje</div>;
};
