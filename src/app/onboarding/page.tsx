import { UpdateProfile } from "@/components/utils/UpdateProfile";

export default function Onboarding() {
  return (
    <main className="relative h-full w-full flex flex-col p-4">
      <UpdateProfile />
    </main>
  );
}
